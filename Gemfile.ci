#!/usr/bin/env bundle

source 'https://rubygems.org'

group :default do
  gem 'bourbon', '~> 4.0.2'

  gem 'sprockets', '~> 2.12.1'
end

group :development do
  gem 'rake', '~> 10.3.2'
end

group :development, :test do
  gem 'rspec', '~> 3.0.0'
end