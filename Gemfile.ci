#!/usr/bin/env bundle

source 'https://rubygems.org'

group :default do
  gem 'sass', '>= 3.2'

  gem 'sprockets', '~> 2.12'
end

group :development do
  gem 'rake', '~> 10.3'
end

group :development, :test do
  gem 'rspec', '~> 3.0'
end