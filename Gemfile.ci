#!/usr/bin/env bundle

source 'https://rubygems.org'

group :default do
  gem 'sass', '>= 3.2.12'

  gem 'sprockets', '~> 2.10.1'
end

group :development do
  gem 'bundler', '~> 1.5.3'

  gem 'rake', '~> 10.1.1'
end

group :development, :test do
  gem 'rspec', '~> 2.14.1'
end