#!/usr/bin/env bundle

source 'https://rubygems.org'

group :default do
  gem 'sass',      '~> 3.2.10'

  gem 'sprockets', '~> 2.10.0'
end

group :development do
  gem 'bundler', '~> 1.3.5'

  gem 'rake', '~> 10.1.0'
end

group :development, :test do
  gem 'rspec', '~> 2.14.1'
end